import { Pagination } from '@mui/material';

const Games = () => {
  return (
    <div>
      <Pagination
        count={10}
        variant='outlined'
      />
    </div>
  );
};

export default Games;
